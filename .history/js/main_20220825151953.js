/*se declaran la funcion a realizar*/
function conversor(){
    let valor = parseInt(document.getElemenById('montoCotizar').value);
    let montoCambiar = montoCotizar;
    /*declaramos el valor de las monedas*/
    let dolar = 300;
    let euro = 250;
    let libra = 350;
    if (document.getElementById(uno)){
    let montoCambiado= montoCambiar / dolar
    }
}

